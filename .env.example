# ============================================================================
# Love Ledger - Environment Variables
# ============================================================================
#
# Copy this file to .env.local and fill in your values:
#   cp .env.example .env.local
#
# IMPORTANT: Never commit .env.local to version control. It contains secrets!
#
# Legend:
#   [REQUIRED] - Must be set for the app to function
#   [OPTIONAL] - Enhanced functionality, but not required for basic operation
#
# For detailed setup instructions, see the README.md file or visit:
# https://github.com/shortforge/love-ledger#environment-setup
#
# ============================================================================

# ============================================================================
# SUPABASE CONFIGURATION (Required)
# ============================================================================
#
# Supabase provides authentication, database (PostgreSQL with PostGIS), and
# real-time subscriptions for Love Ledger.
#
# To get these values:
# 1. Create a free project at https://supabase.com
# 2. Go to Project Settings -> API
# 3. Find your Project URL and API keys
#
# Documentation: https://supabase.com/docs/guides/getting-started
# Get these from your Supabase project settings -> API
# https://app.supabase.com/project/_/settings/api
# ============================================================================

# [REQUIRED] Your Supabase project URL
# Found in: Project Settings -> API -> Project URL
# Format: https://[project-id].supabase.co
#
# This is the base URL for all Supabase API requests.
# It's safe to expose publicly (used in browser).
# Example: https://abcdefghijklmnop.supabase.co
#
# For Expo/React Native:
EXPO_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
# For Next.js:
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co

# [REQUIRED] Supabase Anonymous (Public) Key
# Found in: Project Settings -> API -> Project API Keys -> anon public
#
# This key is safe to use in the browser. It only allows access based on
# your Row Level Security (RLS) policies. Users can only access data they
# are authorized to see.
#
# This key is safe to expose in client-side code because:
# - It only has access allowed by Row Level Security (RLS) policies
# - It cannot bypass database security rules
# - All sensitive operations require authentication
#
# WARNING: Do NOT use the service_role key here!
#
# For Expo/React Native:
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here
# For Next.js:
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# [OPTIONAL] Supabase Service Role Key
# Found in: Project Settings -> API -> Project API Keys -> service_role secret
#
# DANGER: This key bypasses Row Level Security (RLS)!
# Only use this in server-side code (API routes, server actions).
# Never expose this key to the client/browser.
#
# Use cases:
#   - Admin operations that need to bypass RLS
#   - Server-side data migrations
#   - Background jobs/cron tasks
#
# If you're just doing local development, you may not need this initially.
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# ============================================================================
# GOOGLE MAPS CONFIGURATION (Required)
# ============================================================================
#
# Google Maps is used for the interactive map showing missed connection
# locations. Users can browse and post to specific places on the map.
#
# To get your API key:
# 1. Go to Google Cloud Console: https://console.cloud.google.com
# 2. Create a new project (or select existing)
# 3. Enable the following APIs:
#    - Maps SDK for Android
#    - Maps SDK for iOS
#    - Maps JavaScript API
#    - Places API
#    - Geocoding API
# 4. Go to Credentials -> Create Credentials -> API Key
# 5. Restrict your API key to protect against abuse
#
# Documentation: https://developers.google.com/maps/documentation
# Get this from Google Cloud Console -> Credentials
# https://console.cloud.google.com/apis/credentials
# Required APIs: Maps JavaScript API, Places API
# ============================================================================

# [REQUIRED] Google Maps API Key
# Found in: Google Cloud Console -> Credentials
#
# This key is used for venue search, location display, and geocoding.
# Make sure to:
#   - Enable Maps SDK for Android, iOS, Maps JavaScript API, Places API, and Geocoding API
#   - Restrict your API key to your app's bundle ID/domain
#
# Billing Note: Google Maps requires a billing account, but offers $200/month
# free credit which is typically enough for development and small-scale apps.
#
# For Expo/React Native:
EXPO_PUBLIC_GOOGLE_MAPS_API_KEY=your-google-maps-api-key-here
# For Next.js:
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=your-google-maps-api-key

# [REQUIRED] Google Places API Key (Server-side)
# Found in: Google Cloud Console -> Credentials
#
# This key is used for server-side Google Places API calls including:
#   - Place search and autocomplete
#   - Place details retrieval
#   - Nearby search queries
#
# IMPORTANT: This key should be kept secret and only used in server-side code.
# Do NOT expose this key to the client/browser.
#
# Make sure to:
#   - Enable the Places API in Google Cloud Console
#   - Restrict this key to server IP addresses only
GOOGLE_PLACES_API_KEY=your-google-places-api-key

# ============================================================================
# EVENT PLATFORM OAUTH CONFIGURATION (Optional)
# ============================================================================
#
# These OAuth credentials enable integration with event platforms (Eventbrite,
# Meetup) to discover and import events for posting missed connections.
#
# Note: Event integration is optional. The app works without these credentials,
# but users won't be able to browse or import events from these platforms.
#
# ============================================================================

# ----------------------------------------------------------------------------
# EVENTBRITE OAUTH
# ----------------------------------------------------------------------------
#
# To get your Eventbrite OAuth credentials:
# 1. Go to https://www.eventbrite.com/platform/api-keys
# 2. Create a new app or select existing
# 3. Copy the API Key (Client ID) and Client Secret
# 4. Set your OAuth redirect URI to: [your-app-url]/api/auth/callback/eventbrite
#
# Documentation: https://www.eventbrite.com/platform/api
# ----------------------------------------------------------------------------

# [OPTIONAL] Eventbrite OAuth Client ID
# Found in: Eventbrite Developer Portal -> Your App -> API Key
EVENTBRITE_CLIENT_ID=your-eventbrite-client-id

# [OPTIONAL] Eventbrite OAuth Client Secret
# Found in: Eventbrite Developer Portal -> Your App -> Client Secret
#
# DANGER: Keep this secret! Only use server-side, never expose to client.
EVENTBRITE_CLIENT_SECRET=your-eventbrite-client-secret

# ----------------------------------------------------------------------------
# MEETUP OAUTH
# ----------------------------------------------------------------------------
#
# To get your Meetup OAuth credentials:
# 1. Go to https://www.meetup.com/api/oauth/list/
# 2. Create a new OAuth Consumer
# 3. Copy the Key (Client ID) and Secret
# 4. Set your OAuth redirect URI to: [your-app-url]/api/auth/callback/meetup
#
# Documentation: https://www.meetup.com/api/guide/
# ----------------------------------------------------------------------------

# [OPTIONAL] Meetup OAuth Client ID
# Found in: Meetup API -> OAuth Consumers -> Your Consumer -> Key
MEETUP_CLIENT_ID=your-meetup-client-id

# [OPTIONAL] Meetup OAuth Client Secret
# Found in: Meetup API -> OAuth Consumers -> Your Consumer -> Secret
#
# DANGER: Keep this secret! Only use server-side, never expose to client.
MEETUP_CLIENT_SECRET=your-meetup-client-secret

# ============================================================================
# OPTIONAL CONFIGURATION
# ============================================================================
# These variables are optional and have sensible defaults if not set.
# Uncomment and modify if you need to customize behavior.
# ============================================================================

# [OPTIONAL] Enable debug mode for development (default: false in production)
# EXPO_PUBLIC_DEBUG_MODE=true

# [OPTIONAL] Custom API timeout in milliseconds (default: 30000)
# EXPO_PUBLIC_API_TIMEOUT=30000

# ============================================================================
# DEVELOPMENT TOOLS (Optional)
# ============================================================================
#
# These environment variables are useful for local development and debugging.
# They are not required for the app to function.
# ============================================================================

# [OPTIONAL] Enable verbose logging for debugging
# Set to 'true' to see detailed logs in the console
# DEBUG=true